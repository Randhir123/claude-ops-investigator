{
  "mcpServers": {
    "claude-ops-support-agent": {
      "command": "python",
      "args": [
        "-m",
        "claude_ops.mcp.server"
      ],
      "env": {
        "PYTHONPATH": "${PWD}/src"
      }
    }
  }
}
